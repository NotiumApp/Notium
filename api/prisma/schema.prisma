// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")

}

generator client {
  provider = "prisma-client-js"
}

model User {
  uid   String @unique
  notes Note[]
}

model Note {
  id       String  @id @default(uuid())
  title    String
  body     String
  User     User?   @relation(fields: [userUid], references: [uid])
  userUid  String
  children Note[]  @relation("tree")
  Parent   Note?   @relation("tree", fields: [parentId], references: [id], onDelete: Cascade)
  parentId String?
}
